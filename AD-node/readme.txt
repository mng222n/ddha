#
# DHT Overlay - 2008, INT
# 
# Should run this on SuperNodes(SNs)
# Check the directories for more details
#
#

How to run it:
just go to "bin" directory and run as follows (must run as root if using linux os)

./p2psiphoc_run.sh (for enabling DHT & SIP distributed Proxy/Registrar)
./dht_run.sh (for enabling DHT only)